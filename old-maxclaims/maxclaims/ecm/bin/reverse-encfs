#!/bin/sh
ECM_SCRIPT=$(readlink -f "$0")
ECM_SCRIPT_PATH=$(dirname "$ECM_SCRIPT")
ECM_ETC="$ECM_SCRIPT_PATH/../etc/"

encfs --reverse --extpass="cat $ECM_ETC/key/ecm-encfs.key" --standard --public $*
